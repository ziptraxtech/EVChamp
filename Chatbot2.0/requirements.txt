fastapi>=0.100.0
uvicorn[standard]>=0.30.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
python-multipart>=0.0.6

# LangChain RAG stack — pinned to ZipsureAI's versions so the
# create_retrieval_chain / create_stuff_documents_chain helpers exist.
langchain==0.2.16
langchain-community==0.2.16
langchain-core==0.2.38
langchain-pinecone==0.1.3
langchain-text-splitters==0.2.4
pinecone-client==5.0.1
# Slim Gemini SDK — replaces langchain-google-genai (saves ~200 MB of
# google-generativeai/grpcio/protobuf). Custom thin LangChain wrappers
# live in app/services/gemini.py.
google-genai>=1.0.0

# Ingest-only (used by scripts/ingest.py — not at request time)
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
playwright>=1.40.0
